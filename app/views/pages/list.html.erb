<% @page_title = "Pages List" %>

<% if !flash[:notice].blank? %>
	<div class='notice'>
		<%= flash[:notice] %>
	</div>
<% end %>

<div class='Pages list'>
	<h2>Pages</h2>

	<%= link_to("Add New Page", {:action => 'new'}, :class => 'action new') %>

	<table class='listing' summary='page list'>
		<tr class='header'>
			<th>&nbsp;</th>
			<th><%= link_to('Subject', {:controller => 'subjects', :action => 'list'}) %></th>
			<th>Page</th>
			<th>Permalink</th>
			<th>Sections</th>
			<th>Visible</th>
			<th>Actions</th>
		</tr>
		<% @pages.each do |page| %>
		<tr>
			<td><%= page.position %></td>
			<td>
				<% if page.subject.nil? %>
					<%= 'Not yet' %>
				<% else %>
					<%= link_to(page.subject.name, {:controller => 'subjects', :action => 'show', :id => page.subject.id}) %>
				<% end %>
			</td>
			<td><%= page.name %></td>
			<td><%= page.permalink %></td>
			<td><%= page.sections.size %></td>
			<td class='center'><%= page.visible ? 'Yes' : 'No' %></td>
			<td class='actions'>
				<%= link_to('Show', {:action => 'show', :id => page.id}, :class => 'action show') %>
				<%= link_to('Edit', {:action => 'edit', :id => page.id}, :class => 'action edit') %>
				<%= link_to('Delete', {:action => 'delete', :id => page.id}, :class => 'action delete') %>
			</td>
		</tr>
		<% end %>
	</table>
</div>